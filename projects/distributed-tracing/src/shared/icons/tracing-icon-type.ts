export const enum TracingIconType {
  // These values come from Material. We will replace these with our own icon library in the future.
  EntrySpan = 'svg:entry-span',
  ExitSpan = 'svg:exit-span',
  InternalSpan = 'svg:internal-span',
  OpenTracing = 'svg:open-tracing'
}
